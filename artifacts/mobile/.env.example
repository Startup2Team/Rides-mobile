EXPO_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_public_token_here
# Use your machine's LAN IP (not localhost) when testing on a physical device
# Run: ifconfig | grep "inet " | grep -v 127  to find it
EXPO_PUBLIC_API_BASE_URL=http://<YOUR_LAN_IP>:8080/api/v1
EXPO_PUBLIC_WS_BASE_URL=ws://<YOUR_LAN_IP>:8080/api/v1
