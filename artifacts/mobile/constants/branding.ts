/** Display name, bundle IDs, and public URLs for the Rides app. */
export const APP_NAME = 'Rides';

/** iOS bundle ID & Android applicationId (reverse-DNS for rides.rw). */
export const IOS_BUNDLE_IDENTIFIER = 'rw.rides.app';
export const ANDROID_PACKAGE = 'rw.rides.app';

/** Expo deep-link / custom URL scheme */
export const APP_SCHEME = 'rides';

export const APP_DOMAIN = 'rides.rw';
export const WEBSITE_URL = `https://${APP_DOMAIN}`;

export const SUPPORT_EMAIL = `support@${APP_DOMAIN}`;
export const INFO_EMAIL = `info@${APP_DOMAIN}`;
export const SAFETY_EMAIL = `safety@${APP_DOMAIN}`;
export const PRIVACY_EMAIL = `privacy@${APP_DOMAIN}`;

/** Public support line (Rwanda business number). Shown locally, dialed as E.164. */
export const SUPPORT_PHONE_DISPLAY = '+250 796 591 452';
export const SUPPORT_PHONE_E164 = '+250796591452';
/** wa.me format — E.164 without the leading '+'. */
export const SUPPORT_WHATSAPP = '250796591452';

export const TERMS_URL = `https://${APP_DOMAIN}/terms`;
export const PRIVACY_URL = `https://${APP_DOMAIN}/privacy`;
export const OSS_URL = `https://${APP_DOMAIN}/oss`;
